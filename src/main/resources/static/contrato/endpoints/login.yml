post:

  security: []

  tags:
    - Autenticación

  description: |
    **Códigos de error:** `AUTENTICACION_INVALIDA`

  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schema/LoginRequest.yml'

  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schema/LoginResponse.yml'