get:

  tags:
    - Centros de Distribucion

  description: |

    **Permisos requeridos:** `ADMINISTRADOR`

    **Códigos de error:** `CENTRO_NO_ENCONTRADO`

  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer

  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: '../schema/CentroDistribucionResponse.yml'


put:

  tags:
    - Centros de Distribucion

  description: |

    **Permisos requeridos:** `ADMINISTRADOR`

    **Códigos de error:** `CENTRO_NO_ENCONTRADO`

  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
        format: int64

  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schema/CentroDistribucionRequest.yml'

  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: '../schema/CentroDistribucionResponse.yml'


delete:

  tags:
    - Centros de Distribucion

  description: |

    **Permisos requeridos:** `ADMINISTRADOR`

    **Códigos de error:** `CENTRO_NO_ENCONTRADO`

  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
        format: int64

  responses:
    '200':
      description: Eliminado correctamente